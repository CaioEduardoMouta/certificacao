Ser público (public);
Ser estático (static);
Não ter retorno (void);

O método main não pode devolver int nem Void. Ele também deve ser público e só receber um argumento: um array (ou varargs) de String.

Classe no mesmo pacote não precisa

Não pode importar duas classes com o mesmo nome